# Проект 16: Мультиагентная генерация контента (CrewAI + Streamlit)

## 1. Обзор проекта

Проект 16 представляет собой систему автоматизированного создания контента, использующую фреймворк **CrewAI** для оркестрации команды AI-агентов. Веб-интерфейс на базе **Streamlit** позволяет пользователям легко задавать темы, настраивать параметры и получать готовые статьи в формате Markdown.

### Основные возможности
- **Команда агентов**:
  - 🕵️ **Исследователь (Research Analyst)**: Ищет достоверную информацию в интернете, проверяет факты и собирает ссылки.
  - ✍️ **Писатель (Content Writer)**: Создает структурированный черновик статьи на основе отчета исследователя.
  - 📝 **Редактор (Senior Editor)**: Улучшает читабельность, проверяет стиль и форматирует финальный текст.
- **Гибкая настройка**: Выбор аудитории (Audience), тона (Tone), объема слов (Word Count), языка и количества источников.
- **Веб-интерфейс**: Интуитивно понятный UI для запуска и мониторинга процесса.
- **История**: Автоматическое сохранение результатов и возможность просмотра прошлых генераций.

---

## 2. Архитектура

Система построена на следующих компонентах:

- **Streamlit (`app.py`)**: Отвечает за взаимодействие с пользователем, сбор параметров и отображение результатов.
- **CrewAI Pipeline (`crew.py`)**: Связывает агентов и задачи в последовательный процесс (Sequential Process).
- **Агенты (`agents.py`)**: Определяют роли, цели и инструменты (например, поиск в Google через Serper).
- **Задачи (`tasks.py`)**: Описывают конкретные действия, которые должен выполнить каждый агент.

### Поток данных (Workflow)
1. **Input**: Пользователь вводит тему "Искусственный интеллект в медицине".
2. **Research Task**: Исследователь ищет информацию, составляет бриф с 5+ источниками.
3. **Writing Task**: Писатель создает черновик статьи, используя бриф.
4. **Editing Task**: Редактор полирует текст, проверяет структуру и сохраняет ссылки.
5. **Output**: Готовый файл `.md` отображается в UI и сохраняется в папку `output/`.

---

## 3. Установка и Запуск

### Предварительные требования
- Python 3.10 или выше.
- Ключ **OpenAI API** (для работы LLM).
- Ключ **Serper.dev API** (опционально, если нужен поиск в интернете).

### Пошаговая инструкция

1. **Клонирование репозитория**
   ```bash
   git clone <URL_репозитория>
   cd project-16-crewai
   ```

2. **Создание виртуального окружения (рекомендуется)**
   ```bash
   python -m venv venv
   source venv/bin/activate  # Для macOS/Linux
   # venv\Scripts\activate   # Для Windows
   ```

3. **Установка зависимостей**
   ```bash
   pip install -r requirements.txt
   ```

4. **Настройка переменных окружения**
   Создайте файл `.env` в корне проекта (можно скопировать из примера):
   ```bash
   cp .env.example .env
   ```
   Откройте `.env` и вставьте свои ключи:
   ```ini
   OPENAI_API_KEY=sk-proj-...
   SERPER_API_KEY=...
   MODEL_NAME=gpt-4o-mini  # Можно использовать gpt-3.5-turbo или gpt-4
   ```

### Запуск приложения

#### Веб-интерфейс (Рекомендуется)
```bash
streamlit run app.py
```
После запуска откроется браузер по адресу `http://localhost:8501`.

#### Консольный режим (Legacy)
Если вам нужно запустить генерацию без интерфейса (по списку тем в `main.py`):
```bash
python main.py
```

---

## 4. Использование интерфейса

1. **Sidebar (Боковая панель)**:
   - **Topic**: Введите тему статьи (обязательно).
   - **Target Audience**: Укажите целевую аудиторию (например, "Новички", "Эксперты").
   - **Tone**: Выберите тон (Профессиональный, Дружелюбный, Академический).
   - **Output Language**: Выберите язык результата (Russian / English).
   - **Word Count Range**: Задайте желаемый диапазон слов.
   - **Include Web Research**: Включите, если нужен поиск в интернете.
   - **Number of Sources**: Минимальное количество источников.

2. **Основная область**:
   - Нажмите кнопку **Generate Article**.
   - Следите за прогрессом ("Building agents...", "Agents are working...").
   - Процесс может занять 1-3 минуты в зависимости от сложности и скорости API.

3. **Результат**:
   - После завершения появится Markdown-превью статьи.
   - Вы можете скачать файл `.md` по кнопке **Download**.
   - Внизу страницы доступна история предыдущих генераций (папка `output/`).

---

## 5. Структура проекта

```text
project-16-crewai/
├── app.py             # Точка входа для Streamlit
├── agents.py          # Определение агентов (Researcher, Writer, Editor)
├── tasks.py           # Описание задач для агентов
├── crew.py            # Сборка и запуск Crew
├── utils.py           # Утилиты (сохранение файлов, чтение истории)
├── main.py            # CLI версия (для тестов)
├── requirements.txt   # Список библиотек
├── .env.example       # Шаблон конфига
└── output/            # Папка с результатами генерации
    ├── *.md           # Готовая статья (timestamp + slug)
    └── *.json         # Метаданные запуска (тема, аудитория, тон и т.д.)
```

---

## 6. Устранение неполадок (Troubleshooting)

| Проблема | Возможная причина | Решение |
|----------|-------------------|---------|
| **Missing OPENAI_API_KEY** | Не задан ключ в `.env` | Проверьте файл `.env` и наличие ключа. Перезапустите приложение. |
| **Search Error / 401** | Неверный SERPER_API_KEY | Проверьте ключ Serper.dev или отключите галочку "Include Web Research". |
| **Streamlit not found** | Не установлено окружение | Убедитесь, что активирован `venv` и выполнен `pip install -r requirements.txt`. |
| **Ошибки генерации** | Лимиты API или сбой сети | Попробуйте повторить запрос. Если ошибка 429 — проверьте баланс OpenAI. |
